spring:
  application:
    name: meebo_werewolf

  datasource:
    url: jdbc:mysql://localhost:3306/meebo_werewolf
    username: root
    password: kauan123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    fetch-register: true
    register-with-eureka: true
    defaultZone: http://localhost:8761/eureka